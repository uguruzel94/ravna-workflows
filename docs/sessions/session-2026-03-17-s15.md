# Session 15 — 2026-03-17

**Task:** Prospect Research v3.8 — Turkish Dedup + Language Enforcement (Critical Fixes)

## Summary

Implemented three critical fixes to prospect-research SKILL.md based on v3.7 testing errors:
1. **SQL dedup:** Added ⛔ CRITICAL rule to STEP 1.5 Pass 1 + STEP 3 — PostgreSQL `LOWER('İ')` ≠ `LOWER('i')`, so IN list MUST use `translate()`
2. **Language handling:** Added ⛔ MANDATORY LANGUAGE ENFORCEMENT to STEP 0.5 — Turkish input = Turkish output (no English allowed)

## Changes

**File: `.claude/skills/prospect-research/SKILL.md`**

- **STEP 1.5 Pass 1 (before SQL block):** Added 40-line ⛔ CRITICAL SQL RULE block
  - Explain why translate() mandatory: byte-level difference in Turkish chars
  - CORRECT example: both sides use `translate()`
  - WRONG example: skip translate() → duplicates bypass dedup
  - Impact: CDC DEMİR ÇELİK, PARS guaranteed dedup via name matching

- **STEP 3 (before SQL block):** Added 7-line ⛔ CRITICAL SQL RULE block
  - Same translate() requirement for IN list pre-normalization
  - Prevents second occurrence of same bug

- **STEP 0.5 (after line 127):** Added 9-line ⛔ MANDATORY LANGUAGE ENFORCEMENT block
  - Turkish input → ALL messages Turkish (no English allowed)
  - Language locked in at STEP 0.5, enforced for entire execution
  - Rule is NOT optional

**File: `docs/STATE.md`**

- Updated CURRENT STATUS → v3.8 with all three fixes
- Updated LAST SESSION → session 15 with full fix details
- Updated BLOCKERS → marked v3.8 items completed, added SQL test verification
- Updated NEXT ACTION → test with Turkish input, verify CDC/PARS dedup, verify language enforcement
- Added session 15 cost ($0.002)

## Commits

- `f437086` — fix: prospect-research SKILL.md v3.8 — Turkish dedup + language enforcement

## Verification

✅ SQL rule correctly documented: `LOWER(translate('CDC DEMİR ÇELİK', 'çşığüöÇŞİĞÜÖ', 'csiguoCsIGUO')) = 'cdc demir celik'`
✅ Language enforcement rule in place with CRITICAL marker
✅ Both sides of SQL comparison now enforce same normalization
✅ Ready for end-to-end test with Turkish input

## Next Steps

1. Run end-to-end test: `keyword: "tıbbi cihaz distributor", location: "İstanbul", count: 5`
2. Verify Turkish responses (no English messages)
3. Verify CDC/PARS-like companies deduplicate via translate() rule
4. Test language enforcement with English input afterward
